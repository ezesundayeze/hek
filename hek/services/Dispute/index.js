const { createDispute } = require("./createDispute");
const { updateDispute } = require("./updateDispute");
const { findAllDispute, findOneDispute } = require("./findDispute");

module.exports = {
  createDispute,
  updateDispute,
  findOneDispute,
  findAllDispute,
};
